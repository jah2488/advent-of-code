def test
  <%= test_data %>
end

puzzle <%= "#{puzzle_no}.1" %>, mode: :count, answer: :unknown do |input, total|
  input.each_line do |line|

  end
end

puzzle <%= "#{puzzle_no}.2" %>, mode: :count, answer: :unknown do |input, total|
  input.each_line do |line|

  end
end
